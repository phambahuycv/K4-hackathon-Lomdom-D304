{
  "name": "vlearn-dashboard-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c blue,green \"python -m uvicorn backend.main:app --reload --port 8000\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "start:api": "python -m uvicorn backend.main:app --port 8000",
    "start:web": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
